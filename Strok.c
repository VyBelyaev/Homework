#include <stdio.h>
#include <memory.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
char stroka[10000];
int len[10000];
int i= 0;
printf("\nEnter your line :");
scanf("%s",stroka);

for(i=0;i< stroka[i] !='\0';i++)
   sym[stroka[i]]++;

printf("\n");
for(i = 0;i < 10000;i++){
   if(len[i]>0){
      printf("\n %c - %d" , i,len[i]);
}
}
}












